// config.js
module.exports = {
  // How many tokens the bucket can hold at maximum
  BUCKET_CAPACITY: 10,

  // How many tokens are added per second
  REFILL_RATE: 2,

  // How often (in milliseconds) to refill tokens
  REFILL_INTERVAL_MS: 1000,

  // Port for the server
  PORT: 3000,
};